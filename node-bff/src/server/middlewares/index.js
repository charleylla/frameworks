const ErrorMiddleware = require('./error.middleware')
const LoggerMiddleware = require('./logger.middleware')
const PjaxMiddleware = require('./pjax.middleware')

module.exports = {
  ErrorMiddleware,
  LoggerMiddleware,
  PjaxMiddleware
}